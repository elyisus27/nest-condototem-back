services:
 condototem-svc:
    image: jesus2787/nest-condovivetotem-back:debian
    container_name: condovive-totem-debian
    restart: unless-stopped
    privileged: true
    volumes:
      - /dev/bus/usb:/dev/bus/usb
      #- adb_data:/root/.android #no lo quiere en windows necesario en orange
    environment:
      DB_TYPE: mysql
      DB_HOST: 192.168.196.1
      DB_PORT: 3307
      DB_USER: root
      DB_PASS: Fr4cCP455w0rd.2025
      DB_NAME: tablethorizontal
      DATABASE_SYNC: 1
      JWT_SECRET: R3s3rv4
      
      APP_PORT: 3000

    ports:
      - "3000:3000"
    